import { NavBar } from "@/components/NavBar";
import { Navigation } from "@/components/Navigation";

const page = () => {
  return (
    <section>
      <NavBar />
      <Navigation />
    </section>
  );
};
export default page;
